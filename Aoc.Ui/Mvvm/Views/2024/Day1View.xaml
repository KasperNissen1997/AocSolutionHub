<Page x:Class="Aoc.Ui.Mvvm.Views._2024.Day1View"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
  xmlns:local="clr-namespace:Aoc.Ui.Mvvm.Views._2024" 
         xmlns:_2024="clr-namespace:Aoc.Ui.Mvvm.ViewModels._2024" 
         d:DataContext="{d:DesignInstance Type=_2024:Day1VM}"
         mc:Ignorable="d" 
  d:Background="White"
  d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox AcceptsReturn="True" TextWrapping="WrapWithOverflow" Width="200" Height="50" Grid.Row="1" Grid.Column="1" VerticalContentAlignment="Center" Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}" Cursor="IBeam" />

        <Button Width="80" Grid.Column="3" Grid.Row="1" Height="50" Command="{Binding SolveCommand}" Content="Solve!" />
    </Grid>
</Page>
